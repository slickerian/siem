# Files to protect (used by tamper_protection.py)
protected_files:
  - config/paths.yml
  - config/rules.yml
  - config/response_actions.yml

# Directories/files to monitor (used by file_watcher.py)
monitored_paths:
  - /home/mechanic/test

white_list_files:
  - /var/log/lightdm
  - /etc/resolv.conf